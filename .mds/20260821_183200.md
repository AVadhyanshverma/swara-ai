# Work Log: Model Switching

## Architecture Decisions / Proxy Routing Modifications
- Replaced `moonshotai/Kimi-K3` with `moonshotai/Kimi-K2.6` across the board to address capacity and rate-limiting issues experienced on the backend proxy server.
- The `Kimi-K2.6` model is now explicitly configured as the handler for `research`, `math`, and `browser` task intents inside `agent_dir/model_router.py`.
- Also updated the automated proxy testing scripts (`proxy_server/client.py`) to stream test `Kimi-K2.6` instead.

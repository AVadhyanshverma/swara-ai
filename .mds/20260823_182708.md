# Work Log - 2026-08-23 18:27:08

## Overview

Removed the remaining stale fully-local privacy claims from the active SWARA onboarding experience and prompt identity sections. The wording now consistently describes a local-first, cloud-assisted architecture.

## Changes

- `ui_story/index.html`
  - Replaced the cinematic onboarding claim that SWARA was a sovereign system.
  - Replaced the claim that the system runs on the user's machine without qualification.
  - Updated the intro to say that history and workspace files stay local by default.
  - Added explicit disclosure that hosted models and selected tools are used when applicable.
- `agent_dir/prompts/system_prompt.txt`
  - Replaced the identity bullets claiming all processing was local and that data never leaves the machine.
  - Clarified that local history and workspace files stay on the user's machine by default.
  - Clarified that hosted models and selected tools may receive data when used.
- `agent_dir/prompts/chat_prompt.txt`
  - Applied the same accurate local-first/cloud-assisted identity wording.

## Architecture Position

SWARA is presented as a local-first AI workspace:

- Chat history and workspace files are local by default.
- Chat history is encrypted locally with hardware-bound keys.
- Model inference and selected research, browser, document, or image tools may use hosted services.

## Validation

- Searched the active onboarding page and prompt files for `runs entirely locally`, `User data never leaves`, `Your data never leaves`, `A sovereign AI`, `Running on your machine`, `privacy-sovereign`, and `100% Local`: no matches remain.
- Compiled `agent_dir/agent.py`, `agent_dir/model_router.py`, and `ui/main.py` with the project virtual environment: passed.
